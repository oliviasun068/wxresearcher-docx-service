services:
  - type: web
    name: wxresearcher-docx-service
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python docx_service/app.py --host 0.0.0.0
    plan: free
